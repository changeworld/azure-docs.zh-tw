---
author: alkohli
ms.service: storsimple
ms.topic: include
ms.date: 10/26/2018
ms.author: alkohli
ms.openlocfilehash: 93f4f74d435cc14130668da102d1246c5fad5872
ms.sourcegitcommit: 2ec4b3d0bad7dc0071400c2a2264399e4fe34897
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/28/2020
ms.locfileid: "67174377"
---
| 限制識別碼 | 限制 | 註解 |
| --- | --- | --- |
| 儲存體帳戶認證的數目上限 |64 | |
| 磁碟區容器的數目上限 |64 | |
| 磁碟區的數目上限 |255 | |
| 每個頻寬範本之排程的數目上限 |168 |每週每一天每小時的排程。 |
| 實體裝置上之分層磁碟區的大小上限 |64 TB 用於斯托簡單 8100 和 StorSimple 8600 |StorSimple 8100 和 StorSimple 8600 是物理設備。 |
| Azure 中的虛擬裝置上之分層磁碟區的大小上限 |30 TB 用於斯托簡單 8010 <br></br> 64 TB 用於斯托簡單 8020 |StorSimple 8010 和 StorSimple 8020 是 Azure 中分別使用標準存儲和高級存儲的虛擬裝置。 |
| 實體裝置上之固定在本機的磁碟區的大小上限 |9 TB 用於 StorSimple 8100 <br></br> 24 TB 用於斯托簡單 8600 |StorSimple 8100 和 StorSimple 8600 是物理設備。 |
| iSCSI 連線的數目上限 |512 | |
| 從起始端之 iSCSI 連線的數目上限 |512 | |
| 每個裝置的存取控制記錄的數目上限 |64 | |
| 每個備份原則的磁碟區數目上限 |24 | |
| 每個備份原則保留的備份數目上限 |64 | |
| 每個備份原則的排程數目上限 |10 | |
| 每個磁碟區可以保留之任何類型快照集的數目上限 |256 |此金額包括本地快照和雲快照。 |
| 可以在任何裝置中呈現的快照集數目上限 |10,000 | |
| 可以針對備份、還原或複製進行平行處理的磁碟區數目上限 |16 |<ul><li>如果卷超過 16 個，則在處理槽可用時按連續處理它們。</li><li>在操作完成之前，無法對克隆卷或已還原的分層捲進行新的備份。 對於本地卷，允許在卷連線後進行備份。</li></ul> |
| 分層磁碟區的還原和複製復原時間 |<2 分鐘 |<ul><li>無論卷大小如何，該卷在還原或克隆操作後的 2 分鐘內可用。</li><li>由於大多數資料和中繼資料仍駐留在雲中，因此卷性能最初可能比平常慢。 隨著資料從雲流向 StorSimple 設備，性能可能會提高。</li><li>下載中繼資料的總時間取決於配置的磁碟區大小。 中繼資料會於背景自動帶入裝置，速率為每 TB 的配置磁碟區資料 5 分鐘。 此速率可能受雲的 Internet 頻寬影響。</li><li>所有中繼資料均位於裝置時，還原或複製作業即已完成。</li><li>在還原或克隆操作完全完成之前，無法執行備份操作。 |
| 固定在本機的磁碟區的還原復原時間 |<2 分鐘 |<ul><li>無論磁碟區大小為何，磁碟區均可在 2 分鐘內用於還原或複製作業。</li><li>由於大多數資料和中繼資料仍駐留在雲中，因此卷性能最初可能比平常慢。 隨著資料從雲流向 StorSimple 設備，性能可能會提高。</li><li>下載中繼資料的總時間取決於配置的磁碟區大小。 中繼資料會於背景自動帶入裝置，速率為每 TB 的配置磁碟區資料 5 分鐘。 此速率可能受雲的 Internet 頻寬影響。</li><li>與分層卷不同，如果有本地固定卷，卷資料也會在設備上本地下載。 當所有磁碟區資料都已下載到裝置時，還原作業即已完成。</li><li>還原操作可能很長，完成還原的總時間將取決於預配的本地卷的大小、Internet 頻寬和設備上的現有資料。 在進行還原作業時，允許在固定在本機的磁碟區上進行備份作業。 |
| 精簡還原可用性 |前次容錯移轉 | |
| 從 SSD 層提供的最大用戶端讀/寫輸送量* |920/720 MB/秒，具有單個 10 千兆乙太網網路介面 |MPIO 和兩個網路介面最多兩次。 |
| 從 HDD 層提供的最大用戶端讀/寫輸送量* |120/250 MB/秒 | |
| 從雲層提供服務時，最大用戶端讀/寫輸送量* |11/41 MB/秒 |讀取輸送量取決於用戶端產生和維護足夠的 I/O 佇列深度。 |

&#42;每個 I/O 類型的最大輸送量以 100% 讀取和 100% 寫入方案來衡量。 實際輸送量可能較低，具體取決於 I/O 組合和網路條件。

